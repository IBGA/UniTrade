export const browseAllItemsSteps = ({ given, and, when, then }) => {
  and('user is on any page', () => {
    assert.fail('Not implemented yet');
  });

  when('user clicks on the Browse button', () => {
    assert.fail('Not implemented yet');
  });

  then(
    'user is taken to the Browse page where all items available for sale are listed',
    () => {
      assert.fail('Not implemented yet');
    }
  );
};
